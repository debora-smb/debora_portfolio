import styles from './GrainyBg.module.css';

export default function GrainyBg() {
  return (
    <div className={`${styles.background} w-full h-[100vh] border-[12px] border-black`}>
    </div>

  )
}
